---
phase: 04.1-shopify-cli-toolkit-integration
plan: 03
subsystem: cli-auth
tags: [cli, auth, oauth, shopify-cli, mcp-server]
dependency_graph:
  requires: [04.1-01, 04.1-02]
  provides: [auth-subcommand, backend-aware-lifespan]
  affects: [cli.py, server.py, .env.example]
tech_stack:
  added: []
  patterns: [subprocess-wrapping, cli-verification-query]
key_files:
  created:
    - tests/test_cli_auth.py
  modified:
    - src/shopify_forecast_mcp/cli.py
    - src/shopify_forecast_mcp/mcp/server.py
    - .env.example
decisions:
  - Patched mock targets at module level (shopify_forecast_mcp.cli.shutil.which) for correct test isolation
metrics:
  duration: 95s
  completed: 2026-04-16T21:44:38Z
  tasks_completed: 2
  tasks_total: 2
  test_results: "174 passed, 1 skipped, 11 deselected"
---

# Phase 4.1 Plan 03: Auth Subcommand & Server Lifespan Summary

CLI auth subcommand wrapping `shopify store auth` with 4 required scopes, verification via timezone query, and enhanced server lifespan logging with backend type.

## Task Results

| Task | Name | Commit | Status |
|------|------|--------|--------|
| 1 | Add auth subcommand to CLI | ed7d989 | Done |
| 2 | Update MCP server lifespan and .env.example | ecabedf | Done |

## What Was Built

### Task 1: Auth Subcommand
- Added `_run_auth()` to `cli.py` that wraps `shopify store auth --store <store> --scopes read_orders,read_all_orders,read_products,read_inventory`
- After auth, verifies by running `shopify store execute` with a `{ shop { ianaTimezone } }` query
- Checks for Shopify CLI on PATH before attempting auth, provides install instructions if missing
- Parses verification JSON with try/except, validates expected keys (T-04.1-07 mitigation)
- 4 test cases: parser registration, CLI-not-found, success flow, failure flow

### Task 2: Server Lifespan & .env.example
- Server lifespan already used `create_backend(settings)` from Plan 04.1-02 -- no change needed
- Enhanced log message to include `type(backend).__name__` for operational visibility
- Updated `.env.example` to show ACCESS_TOKEN as optional/commented with dual auth path documentation

## Deviations from Plan

### Pre-existing State (Not Deviations)
Plan 04.1-02 already updated `cli.py` to use `create_backend(settings)` + `ShopifyClient(backend, settings)` in both `_run_revenue` and `_run_demand`, and already updated `server.py` lifespan to use the backend factory. Task 1 step 5 and Task 2 steps 1-2 were already complete. Only the auth subcommand addition (Task 1 steps 1-4) and enhanced logging/env updates were needed.

### Auto-fixed Issues
**1. [Rule 1 - Bug] Fixed mock patch targets for test isolation**
- **Found during:** Task 1 (test creation)
- **Issue:** Plan used `@patch("shutil.which")` which patches at the wrong module level
- **Fix:** Used `@patch("shopify_forecast_mcp.cli.shutil.which")` and `@patch("shopify_forecast_mcp.cli.subprocess.run")` to patch at the import site
- **Files modified:** tests/test_cli_auth.py

## Verification

- `uv run pytest tests/test_cli_auth.py -x -q`: 4 passed
- `uv run pytest -x -q -m "not slow"`: 174 passed, 1 skipped, 11 deselected
- `grep -c 'create_backend' src/shopify_forecast_mcp/mcp/server.py`: 2
- `grep 'SHOPIFY_FORECAST_ACCESS_TOKEN' .env.example`: commented out (optional)
- Server imports verified: `from shopify_forecast_mcp.mcp.server import lifespan, mcp`

## Self-Check: PASSED
