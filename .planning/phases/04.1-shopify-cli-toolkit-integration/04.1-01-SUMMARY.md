---
phase: 04.1-shopify-cli-toolkit-integration
plan: 01
subsystem: api
tags: [shopify, graphql, httpx, subprocess, protocol, backend-abstraction]

# Dependency graph
requires:
  - phase: 02-shopify-client
    provides: httpx-based GraphQL client with throttle handling
provides:
  - ShopifyBackend Protocol (4-method contract)
  - DirectBackend (httpx with auth + throttle handling)
  - CliBackend (shopify store execute subprocess)
  - create_backend factory (token/CLI/error selection)
  - execute_graphql async subprocess wrapper
  - ShopifyCliError and ShopifyCliNotFoundError exceptions
affects: [04.1-02-shopify-client-refactor, 04.1-03-auth-cli, 04.1-04-tests]

# Tech tracking
tech-stack:
  added: []
  patterns: [Protocol-based backend abstraction, factory pattern for backend selection, subprocess-based CLI integration]

key-files:
  created:
    - src/shopify_forecast_mcp/core/shopify_backend.py
    - src/shopify_forecast_mcp/core/shopify_exec.py
    - tests/test_backend.py
  modified:
    - src/shopify_forecast_mcp/core/exceptions.py

key-decisions:
  - "Executed Task 2 before Task 1 since shopify_backend.py imports from shopify_exec.py"
  - "Used MagicMock for Settings in factory tests since access_token is still required in config (Plan 02 changes it to optional)"

patterns-established:
  - "ShopifyBackend Protocol: runtime_checkable with post_graphql, post_graphql_mutation, download_url, close"
  - "Factory pattern: create_backend selects DirectBackend (token) or CliBackend (CLI on PATH) or raises error"
  - "Throttle handling extracted as reusable pattern in DirectBackend (same logic as ShopifyClient)"

requirements-completed: [CLI-BACKEND-PROTOCOL, CLI-BACKEND-IMPL, CLI-EXEC, CLI-EXCEPTIONS]

# Metrics
duration: 3min
completed: 2026-04-16
---

# Phase 4.1 Plan 01: Backend Protocol & Implementations Summary

**Dual-backend abstraction layer with ShopifyBackend Protocol, DirectBackend (httpx+throttle), CliBackend (subprocess), and factory selection**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-16T21:30:13Z
- **Completed:** 2026-04-16T21:33:19Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Created ShopifyBackend Protocol with 4 async methods defining the contract for all backends
- Implemented DirectBackend with full httpx auth headers and cost-based throttle retry logic (extracted from shopify_client.py)
- Implemented CliBackend delegating to execute_graphql subprocess wrapper for Shopify CLI integration
- Built create_backend factory with 3-way selection: token -> DirectBackend, CLI on PATH -> CliBackend, neither -> error with setup instructions
- Added ShopifyCliError and ShopifyCliNotFoundError exception classes
- 14 unit tests covering both backends and factory (169 total tests passing, 0 regressions)

## Task Commits

Each task was committed atomically:

1. **Task 2: Create shopify_exec.py and add CLI exception classes** - `2a3f0e8` (feat)
2. **Task 1: Create ShopifyBackend Protocol, DirectBackend, CliBackend, and create_backend factory** - `f800c0f` (feat)
3. **Task 3: Unit tests for DirectBackend, CliBackend, and create_backend factory** - `fb2307e` (test)

## Files Created/Modified
- `src/shopify_forecast_mcp/core/shopify_backend.py` - ShopifyBackend Protocol, DirectBackend, CliBackend, create_backend factory
- `src/shopify_forecast_mcp/core/shopify_exec.py` - execute_graphql async function for CLI subprocess calls
- `src/shopify_forecast_mcp/core/exceptions.py` - Added ShopifyCliError and ShopifyCliNotFoundError
- `tests/test_backend.py` - 14 unit tests for both backends and factory selection

## Decisions Made
- Executed Task 2 (exceptions + exec) before Task 1 (backend) since shopify_backend.py imports from shopify_exec.py -- no functional impact, just build order
- Used MagicMock for Settings in factory tests since access_token is still required in the real Settings class (Plan 02 makes it optional)

## Deviations from Plan

None - plan executed exactly as written. Task execution order was swapped (Task 2 before Task 1) to satisfy import dependencies, but all deliverables match the plan.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Backend abstraction layer complete and tested
- Ready for Plan 02: Refactor ShopifyClient to accept any ShopifyBackend instead of direct httpx
- Ready for Plan 03: Auth CLI command wrapping shopify store auth
- Config change (access_token -> optional) deferred to Plan 02 as designed

---
*Phase: 04.1-shopify-cli-toolkit-integration*
*Completed: 2026-04-16*
