---
phase: 07-distribution-docs
plan: 05
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
  - 07-04
files_modified:
  - docs/RELEASE.md
  - CHANGELOG.md
  - README.md
autonomous: false
requirements:
  - R11.5
  - R12.1
  - R12.2
  - R12.3
  - R12.4
implements:
  - D-19
  - D-20
  - D-22
tags:
  - release
  - manual-validation

must_haves:
  truths:
    - "A maintainer prerequisites checklist exists in `docs/RELEASE.md` covering PyPI Trusted Publisher registration, GHCR visibility flip, and GitHub environment setup."
    - "An rc1 dry-run happens on tag `v0.1.0-rc1` and exercises all 4 legs of D-20: CI green, PyPI pre-release upload, `uvx --prerelease=allow` works on a fresh VM, Docker `:latest-rc` + `:bundled-rc` pushed + pull-testable."
    - "Only after rc1 validates all 4 legs does `v0.1.0` get tagged (non-pre-release PyPI upload + stable `:latest` + `:bundled` tags)."
    - "`CHANGELOG.md` [0.1.0] date is updated to the actual release date before tagging."
    - "The README alpha banner + pinned 'Feedback wanted' GitHub issue constitute the full v0.1.0 announce surface (D-22 repo-only)."
  artifacts:
    - path: "docs/RELEASE.md"
      provides: "Maintainer-facing release checklist: pre-tag prerequisites, rc1 dry-run 4-leg verification, v0.1.0 cut procedure, post-release actions"
      min_lines: 100
  key_links:
    - from: "docs/RELEASE.md rc1 procedure"
      to: ".github/workflows/publish.yml"
      via: "tag push triggers end-to-end validation"
      pattern: "v0\\.1\\.0-rc1"
    - from: "docs/RELEASE.md GHCR visibility step"
      to: "ghcr.io/mcostigliola321/shopify-forecast-mcp package settings"
      via: "manual UI flip post-first-push"
      pattern: "GHCR"
---

<objective>
Document the maintainer release procedure and execute the v0.1.0-rc1 dry-run + v0.1.0 tag cut. This plan is **autonomous: false** because it contains `checkpoint:human-action` and `checkpoint:human-verify` tasks that require manual operator steps (PyPI pending-publisher registration, GHCR visibility flip, tag pushes, live verification from a fresh machine).

Purpose: Implements D-19 (Alpha classifier retained — no pyproject change, just verification), D-20 (rc1 dry-run gating v0.1.0), D-22 (repo-only announce — no external socials). Closes the loop on Plans 2-4 by actually exercising the pipeline.

Output: `docs/RELEASE.md` (the maintainer runbook), updated `CHANGELOG.md` date, and a completed 4-leg rc1 verification with a v0.1.0 tag landed on PyPI + GHCR + GitHub Releases.

**Depends on Plans 02, 03, 04** — the publish workflow, Dockerfile, and docs must all exist for the tag push to succeed end-to-end. Plan 01 is a transitive dependency (TimesFM dep swap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-distribution-docs/07-CONTEXT.md
@.planning/phases/07-distribution-docs/07-RESEARCH.md
@.planning/phases/07-distribution-docs/07-VALIDATION.md
@.planning/phases/07-distribution-docs/07-02-SUMMARY.md
@.planning/phases/07-distribution-docs/07-03-SUMMARY.md
@.planning/phases/07-distribution-docs/07-04-SUMMARY.md
@.github/workflows/publish.yml
@Dockerfile
@CHANGELOG.md
@README.md

<interfaces>
From RESEARCH §"CRITICAL manual prerequisites for the plan to call out explicitly":

1. **PyPI Trusted Publisher registration (pending publisher):** Done once by the project owner in their PyPI account BEFORE the first tag push. Required fields:
   - PyPI project name: `shopify-forecast-mcp`
   - Repo owner: `mcostigliola321`
   - Repo name: `shopify-forecast-mcp`
   - Workflow filename: `publish.yml`
   - Environment name: `pypi` (must match `environment: pypi` in publish.yml's publish-pypi job)
   - URL: https://pypi.org/manage/account/publishing/

2. **GitHub Environment creation:** Create environment named `pypi` under `Settings → Environments` in the repo (required by `environment: pypi` in publish-pypi job). No deployment reviewers configured for v0.1.0 solo launch.

3. **GHCR package visibility flip:** After the first successful Docker push, GHCR defaults the package to PRIVATE. Navigate to `https://github.com/users/mcostigliola321/packages/container/shopify-forecast-mcp/settings` (or `/orgs/mcostigliola321/...` if org-owned) and flip visibility to PUBLIC. This must be done after rc1 (first push) — do NOT wait until v0.1.0. Must be done separately for both image name variants if GHCR treats `:bundled-rc` and `:latest-rc` as separate packages (usually the same package with multiple tags; UI will show one entry).

From D-20 — four legs to validate for rc1:
- (a) CI tests pass on the tag SHA (already green from pre-tag push).
- (b) Publish workflow uploads to PyPI as pre-release — visible on pypi.org/project/shopify-forecast-mcp with the "Pre-release" badge.
- (c) `uvx --prerelease=allow shopify-forecast-mcp@0.1.0rc1` works on a fresh machine (no prior install).
- (d) Docker images `:latest-rc` + `:bundled-rc` publish to GHCR and run (basic `docker run --help` check; `:bundled-rc` works with `--network=none`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write docs/RELEASE.md — maintainer runbook</name>
  <files>docs/RELEASE.md</files>
  <read_first>
    - .planning/phases/07-distribution-docs/07-CONTEXT.md (D-19, D-20, D-22)
    - .planning/phases/07-distribution-docs/07-VALIDATION.md §"Manual-Only Verifications" (the canonical rc1 verification text)
    - .planning/phases/07-distribution-docs/07-RESEARCH.md §"CRITICAL manual prerequisites" and §Validation Architecture "Clone-to-running <5 minutes"
    - .github/workflows/publish.yml (post-Plan-02 — confirm job names for the checklist)
    - CHANGELOG.md (post-Plan-04 — confirm the [0.1.0] date placeholder to update)
  </read_first>
  <action>
Create `docs/RELEASE.md` at repo. This file is the maintainer runbook — it captures every manual step required to cut a release, including both one-time setup and per-release actions.

Write the following content to `docs/RELEASE.md`:

```markdown
# Release Procedure

Maintainer runbook for cutting `shopify-forecast-mcp` releases. Not a user-facing doc — consumers should read [README.md](../README.md) and [SETUP.md](SETUP.md).

## Contents

- [One-time prerequisites](#one-time-prerequisites)
- [Per-release: RC dry-run (v0.1.0-rc1 pattern)](#per-release-rc-dry-run)
- [Per-release: Final tag cut](#per-release-final-tag-cut)
- [Post-release verification](#post-release-verification)
- [Rollback](#rollback)

***

## One-time prerequisites

These three setup steps happen ONCE before the first tag push. Skipping any of them causes `v0.1.0-rc1` to fail.

### 1. PyPI Trusted Publisher registration (pending publisher)

1. Log in to https://pypi.org/manage/account/publishing/ with the maintainer account.
2. Click **Add a new pending publisher**.
3. Fill in:
   - **PyPI Project Name:** `shopify-forecast-mcp`
   - **Owner:** `mcostigliola321`
   - **Repository name:** `shopify-forecast-mcp`
   - **Workflow name:** `publish.yml`
   - **Environment name:** `pypi`
4. Click **Add**.

> "Pending publisher" status means the PyPI project will be created on first successful upload. No manual project creation needed.

### 2. GitHub Environment: `pypi`

1. In the repo, navigate to **Settings → Environments**.
2. Click **New environment**, name it `pypi`.
3. Leave deployment protection rules empty for v0.1.0 (solo maintainer, no reviewers).
4. Save.

This environment name must match `environment: pypi` in `.github/workflows/publish.yml` job `publish-pypi`. PyPI checks the environment name during the OIDC exchange.

### 3. (Deferred to first push) GHCR visibility

GHCR packages default to PRIVATE on first push. After the first successful Docker job (during rc1), you'll flip visibility — see the rc1 checklist below.

***

## Per-release: RC dry-run

Every release starts with an rc (release candidate) tag to exercise the full pipeline. Per D-20, four legs must go green before cutting the stable tag.

### Preparation

1. Confirm `main` branch is green in CI:
   ```bash
   gh run list --workflow=ci.yml --branch=main --limit=1
   ```
2. Confirm CHANGELOG.md is up to date and the date in the `[0.1.0]` heading matches the intended release date:
   ```bash
   head -20 CHANGELOG.md
   ```
   If the date needs updating, commit that first:
   ```bash
   # Update CHANGELOG.md: replace "## [0.1.0] - 2026-04-19" with today's date.
   git commit -am "docs(changelog): set [0.1.0] release date"
   git push origin main
   ```
3. Confirm the local `uv build` produces a PyPI-valid wheel (D-23 safety check):
   ```bash
   rm -rf dist/
   uv build
   uvx --from twine twine check dist/*
   unzip -p dist/*.whl '*/METADATA' | grep Requires-Dist | grep -v git+https  # expect all matches; none should contain git+https
   ```

### Tag the rc

```bash
# Use a lightweight annotated tag with a brief message.
git tag -a v0.1.0-rc1 -m "Release candidate 1 for v0.1.0"
git push origin v0.1.0-rc1
```

This triggers `.github/workflows/publish.yml`. Watch it:

```bash
gh run watch $(gh run list --workflow=publish.yml --limit=1 --json databaseId --jq '.[0].databaseId')
```

### 4-leg verification (D-20)

Verify each leg independently. All 4 must pass before proceeding to v0.1.0.

#### Leg (a): CI green on tag SHA

- `gh run list --workflow=ci.yml --commit=$(git rev-parse v0.1.0-rc1)` shows a green run.
- If red: fix the underlying bug, re-push main, re-tag.

#### Leg (b): PyPI pre-release upload

- `gh run view <publish-run-id> --log | grep -A5 "Publish to PyPI"` shows `uv publish` succeeding.
- Visit https://pypi.org/project/shopify-forecast-mcp/ — `0.1.0rc1` appears with a "Pre-release" badge.
- The page MUST render correctly (README renders; mermaid blocks render as code since PyPI doesn't execute mermaid — that's expected per RESEARCH Pitfall 7).
- Metadata check: `curl -s https://pypi.org/pypi/shopify-forecast-mcp/0.1.0rc1/json | jq '.info.requires_dist'` — no `git+https` URLs.

#### Leg (c): `uvx --prerelease=allow` on fresh machine

Spin up a clean container:

```bash
docker run --rm -it python:3.11-slim bash
# Inside the container:
apt-get update && apt-get install -y curl
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.local/bin/env
uvx --prerelease=allow shopify-forecast-mcp@0.1.0rc1 --help
# Expect: CLI help output, exit 0.
```

Also verify the server starts (stdio) without accessing Shopify (expected to error without env vars, but SHOULD start cleanly):

```bash
uvx --prerelease=allow shopify-forecast-mcp@0.1.0rc1 < /dev/null
# Expect: startup log to stderr, quick exit because stdin closed.
```

#### Leg (d): GHCR images publish + run

**Visibility flip (ONE-TIME, do now):**

1. Visit https://github.com/mcostigliola321/shopify-forecast-mcp/packages (or wherever the owner is).
2. Click the `shopify-forecast-mcp` container package.
3. **Package settings → Change visibility → Public → confirm**.

Then verify both tags pull + run:

```bash
docker pull ghcr.io/mcostigliola321/shopify-forecast-mcp:latest-rc
docker pull ghcr.io/mcostigliola321/shopify-forecast-mcp:bundled-rc

# Lazy variant: needs network for first model download
docker run --rm ghcr.io/mcostigliola321/shopify-forecast-mcp:latest-rc --help
# Expect: CLI help output.

# Bundled variant: works offline
docker run --rm --network=none ghcr.io/mcostigliola321/shopify-forecast-mcp:bundled-rc --help
# Expect: CLI help output, no network errors.

# Multi-arch manifest check
docker buildx imagetools inspect ghcr.io/mcostigliola321/shopify-forecast-mcp:latest-rc
# Expect: Manifests for linux/amd64 AND linux/arm64.
```

#### Clone-to-running stopwatch (Phase 7 success criterion 1)

On a different fresh machine (not the rc1 container above):

1. Start timer.
2. `curl -LsSf https://astral.sh/uv/install.sh | sh`
3. Add the Claude Desktop config snippet from README Quick start (substitute real `SHOPIFY_FORECAST_SHOP` + `SHOPIFY_FORECAST_ACCESS_TOKEN`).
4. Restart Claude Desktop.
5. Ask "What does next month look like?" in a new conversation.
6. Receive a markdown revenue forecast table.
7. Stop timer.

**Target:** ≤ 5:00. Record the time in the rc1 GitHub issue.

### Record rc1 outcome

Open a GitHub issue titled **"v0.1.0-rc1 validation"** and fill in all 4 legs plus the stopwatch time. Close when all pass.

***

## Per-release: Final tag cut

Only proceed once all 4 rc1 legs are green.

1. Update `CHANGELOG.md` — move any new entries from `[Unreleased]` into the `[0.1.0]` section if applicable (for v0.1.0 there shouldn't be any; Plan 04 seeded the whole set).
2. Update the `[0.1.0]` date to today if it drifted.
3. Commit any doc nits discovered during rc1 verification.
4. Tag:

```bash
git tag -a v0.1.0 -m "v0.1.0 — first public alpha"
git push origin v0.1.0
```

5. Watch:

```bash
gh run watch $(gh run list --workflow=publish.yml --limit=1 --json databaseId --jq '.[0].databaseId')
```

6. Expected outputs:
   - PyPI: `shopify-forecast-mcp 0.1.0` appears (NO pre-release badge).
   - GHCR: `:0.1.0`, `:latest`, `:0.1.0-bundled`, `:bundled` tags pushed (stable mutable tags advance; rc tags unchanged per Pitfall 5 mitigation).
   - GitHub Releases: `v0.1.0` release appears, body from `CHANGELOG.md [0.1.0]` section, `dist/*.whl` + `dist/*.tar.gz` attached, NOT marked pre-release.

***

## Post-release verification

1. `uvx shopify-forecast-mcp --help` (without `--prerelease=allow`) resolves cleanly on a fresh machine.
2. `docker pull ghcr.io/mcostigliola321/shopify-forecast-mcp:latest` succeeds (public package).
3. `docker run --rm --network=none ghcr.io/mcostigliola321/shopify-forecast-mcp:bundled --help` works.
4. README banner still reads "⚠️ v0.1.0 Alpha" (don't bump to beta yet — D-19 locks Alpha classifier).
5. Open a pinned GitHub Issue: **"v0.1.0 feedback wanted"** linking to the Release. This + README banner constitute the full announce surface per D-22 (repo-only, no external socials for v0.1.0).

***

## Rollback

If a v0.1.0 tag ships a broken release:

1. **Cannot yank from PyPI** without breaking downstream pins. Instead, cut a `v0.1.1` patch release with the fix.
2. Delete GHCR tags for the bad version: `gh api --method DELETE /orgs/mcostigliola321/packages/container/shopify-forecast-mcp/versions/<version-id>`.
3. Mark the GitHub Release as "pre-release" (flag it in the UI) + add a warning note to the description.
4. In README banner, link to the new patch release's Release notes.

No force-pushing of tags — PyPI and GHCR both keep historical tags regardless.
```

The file includes only placeholder tokens inside code blocks (`shpat_xxx`, `<publish-run-id>`, `<version-id>`) so `test_no_placeholder_tokens_in_docs` passes.
  </action>
  <verify>
    <automated>test -f docs/RELEASE.md &amp;&amp; test "$(wc -l &lt; docs/RELEASE.md)" -gt 100 &amp;&amp; grep -q "Trusted Publisher" docs/RELEASE.md &amp;&amp; grep -q "v0.1.0-rc1" docs/RELEASE.md &amp;&amp; grep -q "4-leg verification" docs/RELEASE.md &amp;&amp; grep -q "GHCR" docs/RELEASE.md &amp;&amp; grep -q "pypi environment" docs/RELEASE.md -i</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RELEASE.md` exists, >100 lines.
    - Contains section "One-time prerequisites" covering all 3 setup items (PyPI Trusted Publisher, GitHub environment, GHCR visibility deferred).
    - Contains explicit "4-leg verification" section matching D-20 legs (a) CI, (b) PyPI, (c) uvx prerelease, (d) Docker.
    - `grep -c "^### " docs/RELEASE.md` returns at least 8 (sub-sections for each leg).
    - Contains the clone-to-running stopwatch procedure (Phase 7 success criterion 1).
    - Contains the rollback section (no PyPI yank — cut a patch instead).
    - `grep -F "ghcr.io/mcostigliola321/shopify-forecast-mcp" docs/RELEASE.md` present multiple times (verification commands).
    - `grep -F "uvx --prerelease=allow" docs/RELEASE.md` present.
    - `grep -F "--network=none" docs/RELEASE.md` present (offline bundled verification).
    - No placeholder tokens outside fenced code blocks.
  </acceptance_criteria>
  <done>Maintainer has a single canonical runbook for every tag cut; all manual steps itemized and verifiable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Complete one-time PyPI + GitHub environment + pre-tag prerequisites</name>
  <what-built>
    - `docs/RELEASE.md` with the full runbook (Task 1).
    - All previous plans' artifacts (publish.yml, Dockerfile, docs, CHANGELOG.md) are on main and CI is green.
  </what-built>
  <how-to-verify>
Before pushing any tag, the MAINTAINER (human) must complete these setup steps IN ORDER. Claude cannot do these — they require human action on external UIs.

### Step 1 — Register pending publisher on PyPI (manual UI step)

1. Log in to https://pypi.org/manage/account/publishing/.
2. Click **Add a new pending publisher**.
3. Fill in EXACTLY (case-sensitive):
   - PyPI Project Name: `shopify-forecast-mcp`
   - Owner: `mcostigliola321`
   - Repository name: `shopify-forecast-mcp`
   - Workflow name: `publish.yml`
   - Environment name: `pypi`
4. Click **Add** and confirm the pending publisher appears in your list.

**Verification:** visit https://pypi.org/manage/account/publishing/ and confirm a row for `shopify-forecast-mcp / mcostigliola321 / publish.yml / pypi`.

### Step 2 — Create GitHub environment (manual UI step)

1. In the repo, **Settings → Environments → New environment**.
2. Name: `pypi` (lowercase, must match publish.yml exactly).
3. Do NOT add deployment protection rules or required reviewers (solo launch).
4. Save.

**Verification:** the environment appears in **Settings → Environments**.

### Step 3 — Local sanity: `uv build` produces a PyPI-valid wheel

Run locally in the repo:

```bash
rm -rf dist/
uv build
uvx --from twine twine check dist/*
# Expect: "PASSED" on wheel + sdist.

# Belt-and-braces D-23 check:
unzip -p dist/*.whl '*/METADATA' | grep '^Requires-Dist:' | grep 'git+https' && \
  echo "FAIL: direct URL in METADATA" || echo "OK: no direct URLs"
```

Expected final line: `OK: no direct URLs`.

### Step 4 — Commit any doc date updates

Before tagging rc1, confirm `CHANGELOG.md` [0.1.0] date is today's date:

```bash
head -12 CHANGELOG.md | grep "^## \[0\.1\.0\]"
```

If the date is old, update it and commit before tagging.
  </how-to-verify>
  <resume-signal>Type "prereqs done" once all 4 steps above are complete, OR type "blocked: <reason>" if any step failed.</resume-signal>
  <files>External UIs: pypi.org/manage/account/publishing/, repo Settings → Environments, CHANGELOG.md</files>
  <action>See `<how-to-verify>` below for the full procedure. This task is human-only (checkpoint:human-action) — Claude cannot execute the external UI steps. The executor agent presents the verification text and waits for the resume-signal.</action>
  <verify>
    <automated>unzip -p dist/*.whl '*/METADATA' | grep -E '^Requires-Dist:' | grep -q timecopilot-timesfm && ! unzip -p dist/*.whl '*/METADATA' | grep -q 'git+https' && echo ok</automated>
  </verify>
  <done>All 3 one-time prerequisites complete; pending publisher registered; GitHub pypi environment exists; CHANGELOG.md [0.1.0] date is current; local uv build + twine check green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Push v0.1.0-rc1 tag and complete 4-leg verification</name>
  <what-built>
    - All one-time prerequisites complete (Task 2).
    - `.github/workflows/publish.yml` wired, `Dockerfile` builds both targets, `docs/RELEASE.md` has the checklist.
  </what-built>
  <how-to-verify>
Execute the rc1 dry-run per `docs/RELEASE.md` §"Per-release: RC dry-run". All 4 legs of D-20 must go green.

### Push the tag

```bash
git tag -a v0.1.0-rc1 -m "Release candidate 1 for v0.1.0"
git push origin v0.1.0-rc1
gh run watch $(gh run list --workflow=publish.yml --limit=1 --json databaseId --jq '.[0].databaseId')
```

### Verify all 4 legs

Follow `docs/RELEASE.md` §"4-leg verification (D-20)" step by step. Record each leg's outcome in a GitHub issue titled **"v0.1.0-rc1 validation"**.

- [ ] **Leg (a):** CI green on tag SHA — `gh run list --workflow=ci.yml --commit=$(git rev-parse v0.1.0-rc1)` shows green.
- [ ] **Leg (b):** PyPI pre-release visible at https://pypi.org/project/shopify-forecast-mcp/ with "Pre-release" badge; METADATA has no `git+https`.
- [ ] **Leg (c):** `uvx --prerelease=allow shopify-forecast-mcp@0.1.0rc1 --help` on a fresh `python:3.11-slim` container exits 0 with help text.
- [ ] **Leg (d):** GHCR visibility flipped to public (one-time); `docker pull` succeeds for both `:latest-rc` and `:bundled-rc`; `docker run --rm --network=none ghcr.io/mcostigliola321/shopify-forecast-mcp:bundled-rc --help` works (offline).

### Clone-to-running stopwatch

Separately from the 4 legs above, do a stopwatch-timed walkthrough of README Quick start on a fresh laptop/VM. Target: ≤ 5 minutes from `curl` install of uv → first "What does next month look like?" markdown response from Claude Desktop. Record the time in the rc1 issue.

### Troubleshooting

- **PyPI upload fails with "Requires-Dist: direct URL not allowed":** Plan 01's D-23 fix regressed. Verify `pyproject.toml` has `timecopilot-timesfm>=0.2,<0.3` and `uv.lock` matches.
- **PyPI upload fails with "environment pypi not found":** Task 2 Step 2 not completed.
- **PyPI upload fails with OIDC exchange error:** Task 2 Step 1 (pending publisher) not completed, or workflow/env name mismatch.
- **Docker build times out on arm64:** RESEARCH Pitfall 4 — known issue; bump job timeout or retry.
- **`:latest-rc` advanced `:latest` tag:** Pitfall 5 — should not happen; verify publish.yml's `enable=` conditionals on metadata-action.
- **wait-for-ci times out:** ci.yml didn't run on the tag SHA (because tag was pushed without prior main push). Push main first, then tag.
  </how-to-verify>
  <resume-signal>Type "rc1 green" when all 4 legs + stopwatch pass, OR "rc1 failed: <leg> <reason>" to iterate.</resume-signal>
  <files>Git tag `v0.1.0-rc1` pushed; GitHub issue "v0.1.0-rc1 validation" records 4-leg outcomes.</files>
  <action>See `<how-to-verify>` below for the full procedure. This task is human-only (checkpoint:human-action) — Claude cannot execute the external UI steps. The executor agent presents the verification text and waits for the resume-signal.</action>
  <verify>
    <automated>git rev-parse v0.1.0-rc1 >/dev/null 2>&1 && gh run list --workflow=publish.yml --commit=$(git rev-parse v0.1.0-rc1) --limit=1 --json conclusion --jq '.[0].conclusion' | grep -q success</automated>
  </verify>
  <done>All 4 legs of D-20 verified green (CI on SHA, PyPI pre-release visible, `uvx --prerelease=allow` works on fresh VM, GHCR multi-arch pull + run). Stopwatch for clone-to-running recorded. Feedback issue body populated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Tag v0.1.0 final release + post-release verification</name>
  <what-built>
    - rc1 validation complete, all 4 legs green.
    - Any rc1-surfaced doc fixes have been committed.
  </what-built>
  <how-to-verify>
### Tag the stable release

```bash
git tag -a v0.1.0 -m "v0.1.0 — first public alpha"
git push origin v0.1.0
gh run watch $(gh run list --workflow=publish.yml --limit=1 --json databaseId --jq '.[0].databaseId')
```

### Verify stable release outcomes

- [ ] **PyPI:** `shopify-forecast-mcp 0.1.0` appears at https://pypi.org/project/shopify-forecast-mcp/ WITHOUT a "Pre-release" badge.
- [ ] **GHCR:** `:0.1.0`, `:latest`, `:0.1.0-bundled`, `:bundled` tags visible. Verify `:latest` now resolves to the 0.1.0 image (not rc1):
  ```bash
  docker buildx imagetools inspect ghcr.io/mcostigliola321/shopify-forecast-mcp:latest | head -20
  ```
- [ ] **GitHub Releases:** `v0.1.0` release appears with body from `CHANGELOG.md [0.1.0]`, `dist/*.whl` + `dist/*.tar.gz` attached, NOT flagged as pre-release.
- [ ] **Fresh-machine install without `--prerelease=allow`:**
  ```bash
  # On a fresh container:
  docker run --rm -it python:3.11-slim bash
  # inside:
  curl -LsSf https://astral.sh/uv/install.sh | sh
  source ~/.local/bin/env
  uvx shopify-forecast-mcp --help
  # Expect: CLI help output. (Without --prerelease=allow, because 0.1.0 is stable.)
  ```
- [ ] **rc tags untouched:** `:latest-rc` and `:bundled-rc` still point at the rc1 images (Pitfall 5 mitigation confirmed).

### Post-release announce (D-22 — repo-only)

1. Open a GitHub Issue titled **"v0.1.0 feedback wanted"**.
2. Body:
   ```
   v0.1.0 is live:
   - PyPI: https://pypi.org/project/shopify-forecast-mcp/
   - GHCR: ghcr.io/mcostigliola321/shopify-forecast-mcp:latest
   - Release notes: [link to GitHub Release]

   Install: `uvx shopify-forecast-mcp` + follow [docs/SETUP.md](docs/SETUP.md).

   Feedback / issues welcome — any friction in setup, missing tools, or docs clarity.
   ```
3. Pin the issue (**Issue → Pin issue**).
4. README banner stays as "⚠️ v0.1.0 Alpha" (D-19 — no beta bump yet).

**Do NOT post externally for v0.1.0** (D-22 repo-only scope). Revisit external announce surface for v0.2.

### Document any issues surfaced

Open follow-up GitHub issues for anything discovered that couldn't be fixed pre-v0.1.0 (e.g. arm64 build flakiness, wait-on-check-action edge cases). Tag as `v0.2.0-milestone`.
  </how-to-verify>
  <resume-signal>Type "v0.1.0 shipped" when all stable-release checks pass + pinned feedback issue created, OR "rollback: <reason>" to invoke the rollback procedure in docs/RELEASE.md.</resume-signal>
  <files>Git tag `v0.1.0`; pinned GitHub issue "v0.1.0 feedback wanted"; updated README banner (still Alpha per D-19).</files>
  <action>See `<how-to-verify>` below for the full procedure. This task is human-only (checkpoint:human-action) — Claude cannot execute the external UI steps. The executor agent presents the verification text and waits for the resume-signal.</action>
  <verify>
    <automated>curl -s https://pypi.org/pypi/shopify-forecast-mcp/json | jq -r '.info.version' | grep -q '^0.1.0$' && gh release list --limit 1 --json tagName,isPrerelease --jq '.[0] | select(.tagName=="v0.1.0" and .isPrerelease==false)' | grep -q v0.1.0</automated>
  </verify>
  <done>Stable v0.1.0 tag landed on PyPI (no pre-release badge), GHCR (:latest + :bundled advanced), GitHub Release (with dist/* attached). rc tags untouched. Pinned feedback issue created. Repo-only announce complete (D-22).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer ↔ PyPI pending publisher UI | One-time manual config; Trusted Publisher OIDC gate established here. |
| Maintainer ↔ GHCR visibility UI | Post-first-push manual flip; otherwise package stays private and end users get auth errors. |
| Git tag (v*) ↔ CI workflow trigger | Author-controlled, reviewable via commit history. No automation can push tags without maintainer authentication. |
| rc1 tag ↔ stable tag | rc1 MUST precede stable (D-20) — no silent promotion. The 4-leg checklist enforces this procedurally; no code-level gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | GHCR package defaulting to private | mitigate | `docs/RELEASE.md` Task 1 + Task 3 explicitly call out the post-first-push visibility flip. `tests/` can't automate this; procedural enforcement only. Accepted residual risk of 5-10 minutes of "private package" confusion on first push — mitigated by "rc first, stable second" workflow. |
| T-07-19 | Spoofing | Tag pushed by someone other than maintainer | accept | GitHub enforces that only authenticated accounts with repo write access can push tags. Trusted Publisher OIDC additionally scopes to the specific workflow file + environment name. No additional hardening in v0.1.0 (no branch protection rules required for solo maintainer). Revisit in v0.2: add `tag protection rules` + require signed tags. |
| T-07-20 | Elevation of Privilege | rc1 skipped; bad release hits PyPI | mitigate | D-20 procedurally requires rc1 dry-run before v0.1.0. No technical gate — `publish.yml` does not check whether a prior rc tag exists. Maintainer discipline enforced by `docs/RELEASE.md` runbook. Acceptable for v0.1.0; automation could be added in v0.2 via a `check-for-rc` step in publish.yml. |
| T-07-21 | Information Disclosure | Real Shopify token in "v0.1.0 feedback wanted" issue | mitigate | Task 4 issue body template uses only docs links; no credentials. Standard GitHub abuse reports + PII scanning catch leaks. |
| T-07-22 | Repudiation | Ambiguous release provenance (which commit became v0.1.0?) | mitigate | Annotated tags (`git tag -a`) include maintainer identity + message. GitHub Release page shows tag SHA. PyPI shows sdist + wheel digests. GHCR shows image digest. Full traceability without sigstore for v0.1.0. |

**Residual accepted:** T-07-03 (procedural GHCR flip), T-07-19 (solo-maintainer trust), T-07-20 (rc1-first procedural) — all v0.1.0-acceptable, documented for v0.2 revisit.
</threat_model>

<verification>
**Phase-level final check** (after Task 4 completion):
1. `gh release list --limit 3` — shows `v0.1.0` as latest, no pre-release flag.
2. `curl -s https://pypi.org/pypi/shopify-forecast-mcp/json | jq '.info.version'` — returns `"0.1.0"`.
3. `docker manifest inspect ghcr.io/mcostigliola321/shopify-forecast-mcp:latest | jq '.manifests | length'` — returns >= 2 (multi-arch).
4. `docker pull ghcr.io/mcostigliola321/shopify-forecast-mcp:bundled && docker run --rm --network=none ghcr.io/mcostigliola321/shopify-forecast-mcp:bundled --help` exits 0.
5. Pinned feedback issue exists on repo.
6. `uv run pytest -x -q` — full test suite still green (no regressions from doc tweaks).
</verification>

<success_criteria>
- [ ] `docs/RELEASE.md` exists and covers one-time prereqs + rc1 procedure + stable cut + post-release + rollback.
- [ ] All 3 manual one-time prerequisites completed (PyPI pending publisher, GitHub environment, CHANGELOG date).
- [ ] rc1 tag pushed; all 4 legs of D-20 validation green.
- [ ] Clone-to-running stopwatch walkthrough completed; time ≤ 5:00 recorded.
- [ ] v0.1.0 stable tag pushed; PyPI + GHCR + GitHub Release all show stable 0.1.0.
- [ ] GHCR visibility flipped to public.
- [ ] rc tags untouched (:latest-rc / :bundled-rc still point at rc1 images; Pitfall 5 verified).
- [ ] README banner still reads "v0.1.0 Alpha" (D-19 retained).
- [ ] Pinned "v0.1.0 feedback wanted" GitHub issue created.
- [ ] No external socials posted (D-22 repo-only).
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution-docs/07-05-SUMMARY.md` per standard template. Include:
- Tag SHAs for v0.1.0-rc1 and v0.1.0.
- Links to PyPI project page, GHCR package page, GitHub Release.
- rc1 4-leg validation outcomes (copy from the rc1 issue).
- Clone-to-running stopwatch time.
- Pinned feedback issue URL.
- Any v0.2.0-milestone issues opened for deferred work surfaced during rc1/release.
- Final confirmation that R11.1-R11.5 + R12.1-R12.5 are all satisfied (brief checklist).
</output>
